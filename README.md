adventureTime
=============

An ASCII-art adventure game engine

To develop:

npm install
make
node app

Make builds the asset. It is served up in a dumb empty Express app.

To run the tests:

npm test
